Lambda Expression to compute volume of Cylinder.

`
import math
volume = lambda radius, height: (math.pi) * (math.pow(radius,2)) * height
`{{execute}}

`
cylinderVolume = volume(3,4)
`{{execute}}

`
print("Volume of Cylinder is : ", cylinderVolume)
`{{execute}}

